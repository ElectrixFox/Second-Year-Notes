\documentclass[10pt, a4paper]{article}
\usepackage{preamble}

\title{Complex Analysis II \\
    \large Problem Class Notes}
\author{Luke Phillips}
\date{January 2026}

\begin{document}

\maketitle

\newpage

\tableofcontents

\newpage

\section{Problems Class \texorpdfstring{$1$}{}}

\begin{problem}
    Find all complex numbers $z$ for which $|z| ^ 2 = z ^ 2$.

    \begin{solution}
        Recall $|z| ^ 2 = z\bar{z}$.
        We want to solve
        \[
        z \cdot \bar{z} = z ^ 2
        \]
        $z = 0$ solves the above,
        $z \neq 0$ divide by $z$ and get
        \[
        \bar{z} = z \iff z \in \R.
        \]
        Since $z = 0$ is a real number we conclude that
        \[
        |z| ^ 2 = z ^ 2 \iff z \in \R.
        \]
    \end{solution}
\end{problem}

\begin{problem}
    If $\omega = \frac{z - 1}{z + 1}$ show that $\Re(\omega) = \frac{|z| ^ 2 - 1}{|z| ^ 2 + 2\Re(z) + 1}$ and $\Im(\omega) = \frac{2\Im(z)}{|z| ^ 2 + 2\Re(z) + 1}$.

    \begin{solution}
        \begin{align*}
            \frac{z - 1}{z + 1} &= \frac{z - 1}{z + 1}\cdot\frac{\overline{z + 1}}{\overline{z + 1}} \\
            &= \frac{z - 1}{z + 1}\cdot\frac{\bar{z} + 1}{\bar{z} + 1} \\
            &= \frac{z\bar{z} - \bar{z} + \bar{z} - 1}{z\bar{z} + \bar{z} + z + 1} \\
            &= \frac{|z| ^ 2 + 2i\Im(z) - 1}{|z| ^ 2 + 2\Re(z) + 1} \\
            &= \frac{|z| ^ 2 - 1 + 2i\Im(z)}{|z| ^ 2 + 2\Re(z) + 1} \\
            &= \frac{|z| ^ 2 - 1}{|z| ^ 2 + 2\Re(z) + 1} + i\frac{2\Im(z)}{|z| ^ 2 + 2\Re(z) + 1}
        \end{align*}
    \end{solution}
\end{problem}

\begin{problem}
    Describe geometrically the subset of $\C$ specified by $|z + 2 - i| < |iz - 1 + 2i|$.

    \begin{solution}
        Useful formula
        \begin{align*}
            |z - \omega| ^ 2 &= (z - \omega)\overline{(z - \omega)} \\
            &= (z - \omega)(\bar{z} - \bar{\omega}) \\
            &= z\bar{z} - \omega\bar{z} - z\bar{\omega} + \omega\bar{\omega} \\
            &= |z| ^ 2 - 2\Re(\bar{z}\omega) + |\omega| ^ 2 \\
            &= |z| ^ 2 - 2\Re(\bar{\omega}z) + |\omega| ^ 2
        \end{align*}
        consequently
        \begin{align*}
            |z + 2 - i| < |iz - 1 + 2i| &\overset{\text{everything is non-negative}}{\iff} |z + (2 - i)| ^ 2 < |iz - 1 + 2i| ^ 2 \\
            &\iff |z - (i - 2)| ^ 2 < |iz - (1 - 2i)| ^ 2 \\
            &\iff |z| ^ 2 - 2\Re((-i -2)z) + |i - 2| ^ 2 < |iz| ^ 2 - 2\Re((1 + 2i)iz) + |1 - 2i| ^ 2 \\
            \intertext{$|iz| ^ 2 = (|i||z|) ^ 2 = |z| ^ 2$,
            $|i - 2| ^ 2 = 5$,
            $|1 - 2i| ^ 2 = 5$}
        \end{align*}
        so the inequality holds if and only if
        \[
        -2\Re((-i - 2)z) < -2\Re((1 + 2i)iz)
        \]
        $\Re(\alpha z) = \alpha\Re(z)$ if $\alpha \in \R$,
        $\Re(i\alpha z) = -\alpha\Im(z)$ if $\alpha \in \R$.
    \end{solution}
\end{problem}

\begin{problem}
    The real axis and the imaginary axis divide $\C$ into four quadrants as follows:
    \[
    \begin{array}{c|c}
         \Omega_2 & \Omega_1  \\
         \hline
         \Omega_3 & \Omega_4
    \end{array}
    \]
    Determine the images of $\Omega_1, \Omega_2, \Omega_3, \Omega_4$ under the exponential map $z \mapsto e ^ z$.

    \begin{solution}
        $e ^ z = \underbrace{e ^ x}_{> 0} \cdot e ^ {iy}$.
        By uniqueness of polar representation
        \[
        |e ^ z| = e ^ x\qquad \Arg(e ^ z) = y\bmod{2\pi}.
        \]
        For a fixed $x$ and $a < y < b$ we get that $e ^ z$ gives us a circle with angular segment of length $\min(b - a, 2\pi)$.
        The half line $x = c$,
        $0 < y < \infty$ goes to the full circle
        \[
        \{z \in \C\,:\,|z| = e ^ c\}.
        \]
        Same for $x = c$,
        $-\infty < y < 0$.
        \[
        \exp(\Omega_1) = \{z \in \C\,:\, e ^ 0 < |z| < e ^ {\infty}\} = \{z \in \C\,:\, 1 < |z| < \infty\} = \exp(\Omega_4).
        \]
        with $0, \infty$ minimal $x$ and
        "maximal"
        $x$ respectively.
        Similarly
        \[
        \exp(\Omega_2) = \{z \in \C\,:\,e ^ {-\infty} < |z| < e ^ 0\} = \{z \in \C\,:\, 0 < |z| < 1\} = \exp(\Omega_3).
        \]
    \end{solution}
\end{problem}

\begin{problem}
    Give examples to illustrate that,
    in general,
    for complex numbers $z, w$,
    \[
    \Log(zw) \neq \Log(z) + \Log(w).
    \]

    \begin{solution}
        \begin{align*}
            \Log(zw) &= \log|zw| + i\Arg(zw) \\
            &= \log(|z||w|) + i\Arg(zw) \\
            &= \log|z| + \log|w| + i\underbrace{\Arg(zw)}_{\Arg(z) + \Arg(w)\bmod{2\pi}}.
        \end{align*}
        On the other hand
        \begin{align*}
            \Log(z) + \Log(w) &= \log|z| + i\Arg(z) + \log|w| + i\Arg(w) \\
            &= \log|z| + \log|w| + i(\Arg(z) + \Arg(w)).
        \end{align*}
        So
        \[
        \Log(zw) = \Log(z) + \Log(w) \iff \Arg(zw) = \Arg(z) + \Arg(w).
        \]
        This doesn't always hold.
        Take $z = w = -i = e ^ {-i\pi / 2}$.
        $\Arg(z) = \Arg(w) = -\pi / 2$,
        $\Arg(zw) = \Arg(-1) = \pi$.
        
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$2$}{}}

\begin{problem}\phantom{}
    \begin{enumerate}[label = (\roman*)]
        \item Show that the set $\{z_0\}$,
        consisting of a single point $z_0 \in \C$,
        is closed.

        \item Show that the closed ball $\bar{B}_{r}(z_0) \coloneqq \{z \in \C\,:\,|z - z_0| \leq r\}$,
        of radius $r > 0$ centred at $z_0 \in \C$,
        is closed.
    \end{enumerate}

    \begin{solution}
        \begin{enumerate}[label = (\roman*)]
            \item To show that $\{z_0\}$ is closed we need to show that $\{z_0\} ^ c = \C \setminus \{z_0\}$ is open.
            Given $z \in \{z_0\} ^ c$ we define $\varepsilon = \frac{|z - z_0|}{2} > 0$.
            We claim that $B_{\varepsilon}(z) \subset \{z_0\} ^ c$.
            Indeed,
            if $w \in B_{\varepsilon}(z)$
            \begin{align*}
                |w - z_0| &= |(w - z) + (z - z_0)| \\
                &\geq ||w - z| - |z - z_0|| \\
                &= |z - z_0| - |w - z| \\
                &\geq |z - z_0| - \varepsilon \\
                &= \frac{|z - z_0|}{2} > 0
            \end{align*}
            so $w \neq z_0$.

            \item We will show that if $\{z_{n}\}_{n \in \N} \subset \bar{B}_r(z_n)$ and $z_n \to z$ as $n \to \infty$ then $z \in \bar{B}_r(z_0)$.
            We have that
            \[
            |z_n - z_0| \leq n
            \]
            and $z_n \to z$ as $n \to \infty$.
            We want to show that
            \[
            |z - z_0| \leq r
            \]
            since then $z \in \bar{B}_r(z_0)$.
            Recall that $f(z) = |z|$ is continuous and using COLT $z_n - z_0 \to z - z_0$ as $n \to \infty$.
            We conclude that
            \[
            |z_n - z_0| \to |z - z_0| \text{ as } n \to \infty.
            \]
            Since
            \[
            0 \leq |z_n - z_0| \leq r\quad \forall n
            \]
            we must have that its limit also satisfies
            \[
            0 \leq |z - z_0| \leq r.
            \]
        \end{enumerate}
    \end{solution}
\end{problem}

\begin{problem}
    In the space $C([a, b])$ of continuous functions defined on a closed interval $[a, b]$
    (for $a < b$),
    let
    \[
    d_1(f, g) \coloneqq \int_{a}^{b}|f(t) - g(t)|\,dt.
    \]
    Show that $d_1$ is a metric on $C([a, b])$.

    \begin{solution}
        Positivity:
        for any $f, g \in C([a, b])$ $|f(t) - g(t)| \geq 0$ $\forall t \in [a, b]$,
        so $\int_{a}^{b}|f(t) - g(t)|\,dt \geq 0$.
        Also
        \[
        d_1(f, g) = 0 \iff \int_{a}^{b}|f(t) - g(t)|\,dt = 0 \iff |f(t) - g(t)| = 0 \quad \forall t \in [a, b] \iff f \equiv g.
        \]
        Symmetry
        \[
        d_1(f, g) = \int_{a}^{b}|f(t) - g(t)|\,dt = \int_{a}^{b}|g(t) - f(t)|\,dt = d_1(g, f).
        \]
        Triangle inequality:
        want to show $\forall f, g, h \in C([a, b])$
        \[
        d_1(f, g) \leq d_1(f, h) + d_1(h, g)
        \]
        \begin{align*}
            \int_{a}^{b}|f(t) - g(t)|\,dt \leq \int_{a}^{b}|f(t) - h(t)|\,dt + \int_{a}^{b}|h(t) - g(t)|\,dt.
        \end{align*}

        Indeed,
        let $f, g, h \in C([a, b])$ then for any $t \in [a, b]$
        \[
        |f(t) - g(t)| \leq |f(t) - h(t)| + |h(t) - g(t)|
        \]
        integrating over $[a, b]$ gives the desired result.
    \end{solution}
\end{problem}

\begin{problem}
    Show that a map $f : \C \to \C$ is continuous at $z_0 \in \C$ if and only if
    \[
    \lim_{n \to \infty}f(z_n) = f(z_0)\quad\text{for every convergent sequence $\{z_n\}_{n \in \N}$ in $\C$ with $z_n \to z_0$}.
    \]

    \begin{solution}
        "$\implies$"
        Assume that $f$ is continuous at $z_0$,
        i.e. for any $\varepsilon > 0$,
        $\exists \delta > 0$ s.t. if $|z - z_0| < \delta$ then $|f(z) - f(z_0)| < \varepsilon$.
        Let $\{z_n\}_{n \in \N}$ be such that $z_n \to z_0$ as $n \to \infty$.
        We would like to show that for any $\varepsilon > 0$,
        $\exists N \in \N$ s.t. if $n > N$ then
        \[
        |f(z_n) - f(z_0)| < \varepsilon.
        \]
        Given $\varepsilon > 0$ we find $\delta > 0$ s.t. if $|z - z_0| < \delta$,
        $|f(z) - f(z_0)| < \varepsilon$.
        Since $z_n \to z_0$ there exists $N \in \N$ s.t. if $n > N$,
        $|z_n - z_0| < \delta$.
        Consequently if $n > N$,
        \[
        |f(z_n) - f(z_0)| < \varepsilon.
        \]
        As $\varepsilon > 0$ was arbitrary,
        this direction is proved.
        
        "$\impliedby$".
        We will show that if $f$ is not continuous at $z_0$,
        then there exists a sequence $\{z_n\}_{n \in \N}$ s.t. $z_n \to z_0$ as $n \to \infty$ but $f(z_n) \not\to f(z_0)$ as $n \to \infty$.
        $\exists \varepsilon_0 > 0$ s.t. $\forall \delta > 0$ $\exists z_{\delta}$ with $|z_{\delta} - z| < \delta$ s.t.
        \[
        |f(z_{\delta}) - f(z_0)| \geq \varepsilon_0.
        \]
        Let $\delta_n = \frac{1}{n}$ and denote $z_n = z\delta_n$ we have that
        \[
        0 \leq |z_n - z_0| < \delta_n = \frac{1}{n} \to 0
        \]
        by squeezing theorem $|z_n - z_0| \to 0$ as $n \to \infty$ i.e. $z_n \to z_0$ as $n \to \infty$.
        However,
        \[
        |f(z_n) - f(z_0)| \geq \varepsilon_0\qquad \forall n \in \N
        \]
        which implies that $f(z_n) \not\to f(z_0)$ as $n \to \infty$.
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$3$}{}}

\begin{problem}
    At which points is the function
    \[
    f(z) = x\cosh{y} + y\sin(iy)\cos{x}
    \]
    differentiable?
    At what point are they holomorphic?

    \begin{solution}
        \begin{align*}
            \sin(iy) &= \frac{e ^ {i(iy)} - e ^ {-i(iy)}}{2i} \\
            &= \frac{e ^ {-y} - e ^ {y}}{2i} \\
            &= i\frac{e ^ y - e ^ {-y}}{2} = i\sinh(y)
        \end{align*}
        which implies $f(x + iy) = \frac{x\cosh(y)}{u(x, y)} + i\frac{\sinh(y)\cos(x)}{v(x, y)}$.

        $u, v \in C ^ 1(z ^ 2)$ and as such by theorem from lectures $f$ is complex differentiable at $z_0 = x_0 + iy_0$ if and only if Cauchy-Riemann equations hold.
        \begin{align*}
            u_x(x, y) &= \cosh(y) & v_y(x, y) &= \cosh(y)\cos(x) \\
            u_y(x, y) &= x\sinh(y) & v_x(x, y) &= -\sinh(y)\sin(x)
        \end{align*}
        \begin{align*}
            u_x = v_y &\iff \cosh(y) = \cosh(y)\cos(x) \\
            &\iff \cosh(y) = 0\text{ or } 1 = \cos(x) \\
            &\iff \cos(x) = 1 &&(\text{as $\cosh(y) \geq 1$}) \\
            &\iff x = 2\pi k\quad k \in \Z.
        \end{align*}

        \begin{align*}
            u_y = -v_x &\iff x\sinh(y) = -(-\sinh(y)\cdot \sin(x)) \\
            &\iff x\sinh(y) = \sin(x)\sinh(y) \\
            &\iff \sinh(y) = 0\text{ or } x = \sin(x) \\
            &\iff y = 0\text{ or } x = \sin(x).
        \end{align*}
        In total $x = 2\pi k$ and $y = 0$ or $x = \sin(x)$ so we conclude $x = 2\pi k$ and $y = 0$ or $2\pi k = \sin(2\pi k) = 0$.
        We find that the set of points on which $f$ is differentiable is:
        \[
        \{z = 2\pi k, k \in \Z\} \cup \{\Re(z) = 0\}.
        \]
        Since there is no open ball around any point in $S$ that is contained in $S$,
        $f$ is not holomorphic at any point
    \end{solution}
\end{problem}

\begin{problem}
    Let $f(z)$ be a holomorphic function.
    Prove the following variants of the zero derivative theorem,
    which says that,
    if any one of the following conditions hold on a domain $D$ then $f(z)$ is constant on $D$
    \begin{enumerate}[label = (\roman*)]
        \item $f(z)$ is a real number for all $z \in D$.

        \item The real part of $f(z)$ is constant on $D$.

        \item The modulus of $f(z)$ is constant on $D$.
    \end{enumerate}

    \begin{solution}
        $\Im(f(z)) = 0$ for all $z \in D$,
        i.e. if $f = u + v$ then $v = 0$.
        Since $f$ is holomorphic the Cauchy-Riemann equations hold and $u_x = v_y = 0$,
        $u_y = -v_x = 0$.
        $u$ is differentiable on $D$ and $u_x = u_y = 0$ on $D$ which is a domain so $u$ is constant.
        This implies $f(z) = u(x, y) + iv(x, y)$ is constant.


        $\Re(f(z)) = c$ for some $c \in \R$ and all $z \in D$,
        i.e. $u(x, y) = c$ which implies $u_x = u_y = 0$ and the proof follows like the previous.


        If $f = u + iv$ we have that $\exists c \geq 0$ s.t. $|f(z)| ^ 2 = c ^ 2$,
        i.e.
        \[
        u(x, y) ^ 2 + v(x, y) ^ 2 = c ^ 2.
        \]
        If $c = 0$ then $u = v = 0$ $\forall z \in D$.
        Assume that $c > 0$.
        We can differentiate our equation with respect to $x$ to get
        \[
        2u(x, y)u_x(x, y) + 2v(x, y)v_x(x, y) = 0.
        \]
        Similarly,
        differentiating with respect to $y$ gives
        \[
        2u(x, y)u_y(x, y) + 2v(x, y)v_y(x, y) = 0.
        \]
        Using the Cauchy-Riemann equations we find that
        \begin{align*}
            uu_x - vu_y &= 0 \\
            uu_y + uu_x &= 0
        \end{align*}
        i.e.
        \[
        \underbrace{\begin{pmatrix}
            u(x, y)  & -v(x, y) \\ v(x, y) & u(x, y)
        \end{pmatrix}}_{A(x, y)}
        \begin{pmatrix}
            u_x(x, y) \\ u_y(x, y)
        \end{pmatrix} = \begin{pmatrix}
            0 \\ 0
        \end{pmatrix}
        \]
        $\det(A(x, y)) = u(x, y) ^ 2 + v(x, y) ^ 2 = c ^ 2 \neq 0$.
        $A(x, y)$ is invertible which implies $A(x, y)\begin{pmatrix}
            u_x \\ u_y
        \end{pmatrix} = \begin{pmatrix}
            0 \\ 0
        \end{pmatrix}$ if and only if $u_x = 0$,
        $u_y = 0$,
        this implies that $u$ is constant and similarly $v$ is constant.
    \end{solution}
\end{problem}

\begin{problem}
    In what subset of the complex plane is $\sinh{z}$ conformal?
    For every point $z_0$ at which the function is not conformal,
    give an example of two paths
    (lines)
    through $z_0$ such that the angle
    (or the orientation of the angle)
    between them is not preserved by $f(z)$ at $z_0$.

    \begin{solution}
        $f(z) = \sinh(z)$ is entire.
        From a theorem in the lecture notes $f$ is conformal at any point $z_0$ s.t. $f'(z_0) \neq 0$.
        We will find when $f'(z) = 0$
        \begin{align*}
            0 = f'(z) = \cosh(z) = \frac{e ^ z + e ^ {-z}}{2} &\iff e ^ z = -e ^ {-z} \\
            &\iff e ^ {2z} = -1 = e ^ {i\pi} \\
            &\iff 2z = \log(1) + i\pi + 2\pi i k\quad k \in \Z
        \end{align*}
        i.e. $z = i\left(\frac{\pi}{2} + \pi k\right)$.
        $f$ is conformal on $\C \setminus \{i(\pi / 2 + \pi k), k \in \Z\}$
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$4$}{}}

\begin{problem}
    Find the image of the unit circle and the unit disc under the \mobius transformation $f(z) = w = \frac{z + i}{z - i}$.

    \begin{solution}
        $f$ is \mobius and as such will take circlines to circlines as well as boundary to boundary.
        A circline will go to a line under $f$ if and only if it contains a point that $f$ takes to $\infty$.
        $f(i) = \infty$ and $i \in \partial \mathbb{D}$,
        so $f(\partial \mathbb{D})$ is a line.
        $-i \in \partial \mathbb{D}$ and $f(-i) = 0$.
        $1 \in \partial \mathbb{D}$ and
        \[
        f(1) = \frac{1 + i}{1 - i} = \frac{(1 + i)(1 + i)}{|1 - i| ^ 2} = \frac{1 ^ 2+ 2i + i ^ 2}{1 ^ 2 + (-1) ^ 2} = i.
        \]
        So $f(\partial \mathbb{D})$ is the imaginary axis.
        Since boundary goes to boundary $\partial f(\mathbb{D}) = f(\partial \mathbb{D})$ which is the imaginary axis,
        $f(\mathbb{D})$ will contain $f(\text{interior point})$ and be bounded by the imaginary axis
        \[
        f(0) = \frac{0 + i}{0 - i} = -1
        \]
        so $f(\mathbb{D}) = \mathbb{H}_L$.
    \end{solution}
\end{problem}

\begin{problem}
    Is there a \mobius transformation which maps the sides of the triangle with vertices at $-1, i$ and $1$ to the sides of an equilateral triangle
    (all sides of equal length)?
    Either give an example of such a \mobius transformation,
    or explain why it is not possible.

    \begin{solution}
        \mobius transformations are conformal and as such angles between curves must be preserved.
        This is not the case here.
    \end{solution}
\end{problem}

\begin{problem}
    Find the \mobius transformation taking the ordered set of points $\{-i, 1, i\}$ to the ordered set of points $\{-i, 0, i\}$.
    What is the image of the unit disc under this map?
    Which point is sent to $\infty$?

    \begin{solution}
        We will use the cross-ratio.
        \[
        (z_0, z_1; z_2, z_3) = \frac{z_0 - z_2}{z_0 - z_3}\cdot \frac{z_1 - z_3}{z_1 - z_2}.
        \]
        If one of the points is $\infty$ we remove the affected parts or write $1$ instead.
        \[
        (z_0, \infty; z_2, z_3) = \frac{z_0 - z_2}{z_0 - z_3}\cdot \frac{1}{1}.
        \]
        Cross ratio is invariant under \mobius so
        \begin{align*}
            (z, -i; -1, i) = (M(z), -i; 0, i) &\iff \frac{z - (-1)}{z - i}\cdot\frac{-i - i}{-i - (-1)} = \frac{M(z) - 0}{M(z) - i}\cdot \frac{-i - i}{-i - 0} \\
            &\iff (z + 1)(-i)(M(z) - i) = (z - i)M(z)(1 - i) \\
            &\iff M(z)(-iz - i) - (z + 1) = M(z)(z - iz - i - 1) \\
            &\iff M(z)(-iz - i - z + iz + i + 1) = z +  1 \\
            &\iff M(z) = \frac{z + 1}{1 - z}.
        \end{align*}
    \end{solution}
\end{problem}

\begin{problem}
    Find a \mobius transformation $f$ from the upper half-plane $\mathbb{H}$ onto the unit disk $\mathbb{D}$ that takes $1 + i$ to $0$ and
    (when considered as a map $\hat{\C} \to \hat{\C}$)
    also takes $1$ to $-i$.
    Give an explicit formula for $f(z)$.

    \begin{solution}
        Recall that the Cayley map $M_C(z)$ takes $\mathbb{H}$ to $\mathbb{D}$.
        \[
        f : \mathbb{H} \to \mathbb{D}
        \]
        so $(M_C) ^ {-1} \circ f : \mathbb{H} \to \mathbb{H}$ by $H2H$ theorem $\exists S \in \mathrm{SL}_2(\R)$ s.t. $(M_C) ^ {-1} \circ f = M_S$ so $f = M_C \circ M_S = M_{CS}$.
        \[
        S = \begin{pmatrix}
            a & b \\ c & d
        \end{pmatrix}
        \]
        $a, b, c, d \in \R$,
        $ad - bc = 1$.
        \[
        C = \begin{pmatrix}
            1 & -i \\ 1 & i
        \end{pmatrix}.
        \]
        \[
        CS = \begin{pmatrix}
            a - ic & b - id \\
            a + ic & b + id
        \end{pmatrix}
        \]
        i.e.
        \[
        f(z) = \frac{(a - ic)z + (b - id)}{(a + ic)z + (b + id)}
        \]
        $a, b, c, d \in \R$,
        $ad - bc = 1$.
        Plug in the points and find appropriate parameters.
    \end{solution}
\end{problem}

\begin{problem}
    Does there exist a biholomorphic map taking the closed upper half of the unit disc onto the closed unit disc;
    i.e. from $\{z \in \C\,:\,|z| \leq 1,\ \Im{z} \geq 0\}$ onto $\{w \in \C\,:\,|w| \leq 1\}$?
    Either find an example of such a map or show that none exists.

    \begin{solution}
        Biholomorphic implies conformal cannot have this.
    \end{solution}
\end{problem}

\begin{problem}
    Consider the region $P \subset \C$ defined by
    \[
    P = \left\{z \in \mathbb{D}\,:\,-\frac{3\pi}{4} < \Arg{z} < \frac{3\pi}{4}\right\}.
    \]
    \begin{enumerate}[label = (\alph*)]
        \item Draw $P$.

        \item Find a biholomorphic map from $P$ onto the upper half-plane $\mathbb{H}$.
        
        \item Find a biholomorphic map from $P$ onto the lower half-plane $\mathbb{H}_{-} \coloneqq \{w \in \C\,:\,\Im(z) < 0\}$.
    \end{enumerate}

    \begin{solution}
        \begin{enumerate}[label = (\alph*)]
            \item Looks like a Pacman from the $-$ve imaginary axis to the $+$ve imaginary axis on the left hand side of the origin.

            \item Open to semicircle on the y-axis using $z ^ {2 / 3}$,
            branch on $\C \setminus \R_{\leq 0}$.

            \item Rotation by $e ^ {-\pi / 2}z$ then $M_C ^ {-1}$ then $z ^ 2$.
        \end{enumerate}
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$5$}{}}

\begin{problem}\phantom{}
    \begin{enumerate}[label = (\roman*)]
        \item Show that for any $\rho > 0$ the sequence $\left\{\frac{1}{nz}\right\}_{n \in \N}$ converges uniformly on $A = \{z \in \C\,:\, |z| \geq \rho\}$.

        \item Does $\left\{\frac{1}{nz}\right\}_{n \in \N}$ converges uniformly on $\C ^ {*} \coloneqq \C \setminus \{0\}$.
    \end{enumerate}

    \begin{solution}
        \begin{enumerate}[label = (\roman*)]
            \item
            \[
            f_n(z) = \frac{1}{nz}
            \]
            for $z \in \C ^ {*}$ we have that
            \[
            f_n(z) \to 0\text{ as } n \to \infty
            \]
            pointwise.
            \[
            |f_n(z) - f(z)| = \left|\frac{1}{nz} - 0\right| = \frac{1}{n|z|} \leq \frac{1}{n\rho} = s_n
            \]
            On $A = \{z \in \C\,:\, |z| > \rho\}$.
            $s_n > 0$,
            $s_n(\rho)$ is $z$ independent and $s_n \to 0$ as $n \to \infty$.
            By theorem from lecture notes $f_n(z) \to f(z)$ as $n \to \infty$ uniformly.

            \item The convergence is not uniform on $\C ^ {*}$.
            To show that we will find $\{z_n\}_{n \in \N}$ in $\C ^ {*}$ such that
            \[
            |f_n(z_n) - f(z_n)| \geq c > 0
            \]
            for some $c > 0$.

            Indeed,
            $z_n = \frac{1}{n}$,
            \[
            |f_n(z_n) - f(z_n)| = \frac{1}{n\cdot\frac{1}{n}} = 1.
            \]
            Also for $z_n = \frac{1}{n ^ 2}$,
            $\{z_n\}_{n \in \N}$ is in $\C ^ {*}$ and
            \[
            |f_n(z_n) - f(z_n)| = n \to \infty\text{ as } n \to \infty.
            \]
            Note:
            $f_n$ converges to $f$ uniformly on $A_{\rho} = \{z \in \C\,:\, |z| > \rho\}$ for any $\rho > 0$.
            $A_{\rho}$ is an open set and any $z_0 \in \C ^ {*}$ belongs to some $A_{\rho}$
            (indeed $z_0 \in A_{|z_0| / 2}$).
            This implies $f_n$ converges to $f$ locally uniformly.
        \end{enumerate}
    \end{solution}
\end{problem}

\begin{problem}
    For every $n \in \N$,
    let $f_n(z) = \sin(z / n)$ for $z \in \C$.
    Show that $\{f_n\}_{n \in \N}$ converges pointwise on $\C$.
    Let $\rho$ be a positive real number.
    Show that $\{f_n\}_{n \in \N}$ converges uniformly on $A = \{z\,:\,|z| \leq \rho\}$.
    Show that $\{f_n\}_{n \in \N}$ does not converge uniformly on $\C$.

    \begin{solution}
        For any $z \in \C$
        \[
        \frac{z}{n} \to 0\text{ as } n \to \infty
        \]
        pointwise.
        This implies due to the continuity of sine
        \[
        \sin\left(\frac{z}{n}\right) \to 0\text{ as } n \to \infty
        \]
        pointwise.

        We have that
        \[
        \sin(z) = \sin(x)\cosh(y) + i\cos(x)\sinh(y).
        \]
        \[
        \sin(z / n) = \sin(x / n)\cosh(y / n) + i\cos(x / n)\sinh(y / n).
        \]
        \begin{align*}
            |\sin(z / n) - 0| &= \sqrt{\sin ^ 2\left(\frac{x}{n}\right)\cosh ^ 2\left(\frac{x}{n}\right) + \cos ^ 2\left(\frac{x}{n}\right)\sinh ^ 2\left(\frac{x}{n}\right)} \\
            &\leq \sqrt{\sin ^ 2\left(\frac{x}{n}\right)\cosh ^ 2\left(\frac{y}{n}\right) + \sinh ^ 2\left(\frac{y}{n}\right)} \\
            &\leq \sqrt{\frac{x ^ 2}{n ^ 2}\cosh ^ 2\left(\frac{y}{n}\right) + \sinh ^ 2\left(\frac{y}{n}\right)} \\
            &= \sqrt{\frac{x ^ 2}{n ^ 2}\cosh ^ 2\left(\frac{|y|}{n}\right) + \left(\sinh\left(\frac{|y|}{n}\right)\right) ^ 2} \\
            &\leq \sqrt{\frac{\rho ^ 2}{n ^ 2}\cosh ^ 2\left(\frac{\rho}{n}\right) + \sinh ^ 2\left(\frac{\rho}{n}\right)}.
        \end{align*}
        $s_n(\rho) > 0$,
        $s_n(\rho)$ is $z$ independent.
        $s_n(\rho) \to 0$ as $n \to \infty$.
        By a theorem from lecture notes,
        $\sin\left(\frac{z}{n}\right)$ converges uniformly to $0$ on $A_{\rho}$ for any $\rho > 0$.
        $\sin\left(\frac{z}{n}\right)$ will not converge to $0$ uniformly on $\C$.
        Indeed,
        $z_n = n$ is in $\C$ and
        \[
        |f_n(z_n) - f(z_n)|\left|\sin\left(\frac{n}{n}\right)\right| = |\sin(1)|
        \]
        can do worse with $z_n = in ^ 2$
        \[
        |f_n(z_n) - f(z_n)| = \sinh(n).
        \]
    \end{solution}
\end{problem}

\begin{problem}
    Prove that $\sum_{n = 0}^{\infty}e ^ {nz}$ converges uniformly on $A = \{z \in \C\,:\,\Re(z) \leq -1\}$,
    but not on $B = \{z \in \C\,:\, \Re(z) \leq 0\}$.

    \begin{solution}
        $f_n(z) = e ^  {nz}$.
        \[
        |f_n(z)| = |e ^ {nz}| = e ^ {\Re(nz)} = e ^ {n\Re(z)}
        \]
        on $A$
        \[
        |f_n(z)| = e ^ {n\Re(z)} \leq e ^ {-n} = M_n
        \]
        $M_n > 0$,
        $M_n$ is $z$ independent.
        \[
        \sum_{n = 1}^{\infty}M_n = \sum_{n = 1}^{\infty}e ^ {-n} < \infty
        \]
        geometric series with $q = e ^ {-1} < 1$.
        By Weierstrass $M$-test.
        \[
        \sum_{n = 1}^{\infty}f_n(z)
        \]
        converges uniformly on $A$.

        If $\Re(z) = 0$
        \[
        |f_n(z)| = e ^ {n\cdot 0} = 1 \to 0\text{ as } n \to \infty
        \]
        so $\sum_{n = 1}^{\infty}f_n(z)$ doesn't converge pointwise when $\Re(z) = 0$ so $\sum f_n(z)$ cannot converge uniformly on $B$.
    \end{solution}
\end{problem}

\begin{problem}
    Let $R$ satisfy $0 < R < 1$.
    Show that the series $\displaystyle\sum_{n = 1}^{\infty}\frac{z ^ n}{1 + z ^ n}$ converges uniformly on $A = \{z \in \C\,:\,|z| < R\}$.
    Conclude that the infinite series defines a continuous function on the unit disc $\mathbb{D}$.

    \begin{solution}
        \[
        \left|\frac{z ^ n}{1 + z ^ n}\right| = \frac{|z| ^ n}{|1 + z ^ n|} \leq \frac{R ^ n}{|1 + z ^ n|}.
        \]
        The reverse triangle inequality states
        \[
        |1 + z ^ n| \geq ||1| - |z ^ n|| = |1 - |z| ^ n|
        \]
        on $A_R$,
        $|z| < R < 1$ so $|z| ^ n < R ^ n < 1$ so
        \[
        |1 + z ^ n| \geq 1 - |z| ^ n \geq 1 - R ^ n.
        \]
        We conclude that on $A_R$
        \[
        \left|\frac{z ^ n}{1 + z ^ n}\right| \leq \frac{R ^ n}{1 + R ^ n} = M_n(R).
        \]
        $M_n(R) > 0$,
        $M_n(R)$ is $z$ independent
        (in $A_R$).
        \[
        \frac{M_{n + 1}(R)}{M_n(R)} = \frac{R(1 - R ^ {n + 1})}{1 - R ^ n} \to R\text{ as } n \to \infty
        \]
        so $R < 1$ thus $\sum_{n = 1}^{\infty}M_n(R) < \infty$ and by Weierstrass $M$-test $\sum_{n = 1}^{\infty}\frac{z ^ n}{1 + z ^ n}$ converges uniformly on $A_R$.
        As $R < 1$ is arbitrary this shows local uniform convergence in $\mathbb{D}$.
        As $f_n(z) = \frac{z ^ n}{1 + z ^ n}$ is continuous on $\mathbb{D}$,
        the theorem from lecture notes implies the limit of the series is continuous on $\mathbb{D}$.
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$6$}{}}

\begin{problem}
    Let $\gamma$ be the curve $\gamma(\theta) = 2e ^ {i\theta}$,
    with $\theta \in [0, 2\pi]$.
    Show that
    \[
    \left|\int_{\gamma}\frac{e ^ z}{z ^ 2 + 1}\right| \leq \frac{4\pi}{3}e ^ 2.
    \]

    \begin{solution}
        \[
        \left|\int_{\gamma}\frac{e ^ z}{z ^ 2 + 1}\right| \leq L(\gamma)\sup_{z \in \gamma}\left|\frac{e ^ z}{z ^ 2 + 1}\right|
        \]
        $L(\gamma) = 4\pi$ as circle.
        For $z \in \gamma$,
        so points with $|z| = 2$,
        \[
        \left|\frac{e ^ z}{z ^ 2 + 1}\right| = \left|\frac{e ^ {x + iy}}{z ^ 2 + 1}\right| = \frac{e ^ x}{|z ^ 2 + 1|} \leq \frac{e ^ x}{||z| ^ 2 - 1|} = \frac{e ^ x}{3} \leq \frac{e ^ 2}{3}.
        \]
    \end{solution}
\end{problem}

\begin{problem}
    Evaluate the following contour integrals:
    \[
    \int_{|z + i| = 3}\frac{\sin(z)}{z + i}\,dz.
    \]
    \[
    \int_{|z| = 2}\frac{1}{z ^ 2 + 1}\,dz.
    \]

    \begin{solution}
        \[
        f(z) = \sin(z)
        \]
        using CIF
        \[
        \int_{|z + i| = 3}\frac{\sin(z)}{z + i}\,dz = 2\pi i \sin(-i) = 2\pi i\sinh(1).
        \]

        \[
        \frac{1}{z ^ 2 + 1} = \frac{1}{(z - i)(z + i)} = \frac{A}{z - i} + \frac{B}{z + i} = \frac{1}{2i}\frac{1}{z - i} - \frac{1}{2i}\frac{1}{z + i}
        \]
        $A = \frac{1}{z + i}\big|_{i} = \frac{1}{2i}$,
        $B = \frac{1}{z - i}\big|_{-i} = -\frac{1}{2i}$.
        \[
        \int_{|z| = 2}\frac{1}{z ^ 2 + 1}\,dz = \frac{1}{2i}\int_{|z| = 2}\frac{1}{z - i}\,dz - \frac{1}{2i}\int_{|z| = 2}\frac{1}{z + i}\,dz
        \]
        by CIF both $2\pi i \cdot 1$ so integral is $0$.
    \end{solution}
\end{problem}

\begin{problem}
    Show the following function does not have a holomorphic anti-derivative on the indicated domain
    \[
    \frac{z}{1 + z ^ 2}\qquad(1 < |z| < \infty).
    \]

    \begin{solution}
        By Newton-Leibniz just need to show one integral is not zero
        (closed contour)
        $\gamma$ circle radius $2$.
        \[
        \frac{z}{z ^ 2 + 1} = \frac{1}{2i}\frac{z}{z - i} - \frac{1}{2i}\frac{z}{z + i}.
        \]
        \[
        \int_{|z| = 2}\frac{z}{z ^ 2 + 1}\,dz = \frac{1}{2i}\int_{|z| = 2}\frac{z}{z - i}\,dz - \frac{1}{2i}\int_{|z| = 2}\frac{z}{z + i}\,dz = \frac{1}{2i}\cdot 2\pi i\cdot i - \frac{1}{2i}\cdot 2\pi i\cdot (-i) = \pi i - (-\pi i) = 2\pi i \neq 0.
        \]
    \end{solution}
\end{problem}

\begin{problem}\phantom{}
    \begin{enumerate}[label = (\roman*)]
        \item Explain why the domain $\C \setminus \R_{\leq 0}$ is starlike,
        where as usual $\R_{\leq 0} = \{x \in \R\,:\,x \leq 0\}$.

        \item Let $z = re ^ {i\theta}$ be a point in $\C \setminus \R_{\leq 0}$ such that $r > 0$.
        Consider the closed contour $\gamma$ in $\C \setminus \R_{\leq 0}$ consisting of the following three curves:
        straight line segment $[1, r]$ in $\R$ connecting $1$ to $r$.
        Circular arc
        (radius $r$,
        centred at origin)
        in $\C \setminus \R_{\leq 0}$ connecting $r$ with the point $z$.
        Straight line segment $[z, 1]$ reconnecting $z$ to $1$.

        Use Cauchy-Goursat to show
        \[
        \int_{[1, z]}\frac{1}{w}\,dw = \Log(z),
        \]
        where $\Log$ denotes the principal branch of logarithm and $[1, z]$ is the straight line connecting $1$ to $z$.
    \end{enumerate}

    \begin{solution}\phantom{}
        \begin{enumerate}[label = (\roman*)]
            \item Diagram sees it.

            \item Note $\frac{1}{w}$ is holomorphic on $\C \setminus \R_{\leq 0}$.
            So by Cauchy-Goursat starlike
            \[
            \int\frac{1}{w}\,dw = 0
            \]
            with the integral being over the whole shape
            (denoted $\psi$).
            \[
            0 = \int_{\psi}\frac{1}{w}\,dz = \int_{[1, r]}\frac{1}{w}\,dw + \int_{\gamma}\frac{1}{w}\,dw - \int_{[1, z]}\frac{1}{w}\,dw
            \]
            so
            \[
            \int_{[1, 2]}\frac{1}{w}\,dw = \int_{[1, r]}\frac{1}{w}\,dw + \int_{\gamma}\frac{1}{w}\,dw = \ln{r} + i\Arg(\theta) = \Log(z).
            \]
        \end{enumerate}
    \end{solution}
\end{problem}

\begin{problem}
    Find Taylor series about $0$ of:
    \[
    f(z) = \frac{1}{1 - z ^ 2}.
    \]
    \[
    f(z) = \frac{1}{(1 - z) ^ 2}
    \]

    \begin{solution}
        \[
        \frac{1}{1 - z ^ 2} = \frac{1}{1 - (z ^ 2)} = \sum_{n = 0}^{\infty}(z ^ 2) ^ n = \sum_{n = 0}^{\infty}z ^ {2n}.
        \]

        \[
        \frac{1}{(1 - z) ^ 2} = \frac{d}{dz}\frac{1}{1 - z} = \frac{d}{dz}\sum_{n = 0}^{\infty}z ^ n = \sum_{n = 1}^{\infty}nz ^ {n - 1}.
        \]
    \end{solution}
\end{problem}

\begin{problem}
    Let $D \subseteq \C$ be a starlike domain.
    For each $n \geq 1$,
    suppose $f_n : D \to \C$ is holomorphic,
    and suppose that the sequence $\{f_n\}_{n \in \N}$ converges uniformly on $D$ to some limit function $f : D \to \C$.
    Show that $f$ is holomorphic on $D$.

    \begin{solution}
        Know by Cauchy-Goursat that $\int_{\gamma} f_n\,dz = 0$ for all closed contours in $D$.
        So for any closed $\gamma$
        \[
        \int_{\gamma}f(z)\,dz = \int_{\gamma}\lim_{n \to \infty}f_n(z)\,dz = \lim_{n \to \infty}\int_{\gamma}f_n(z)\,dz = \lim_{n \to \infty}0 = 0.
        \]
        By Morera $f$ is holomorphic.
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$7$}{}}

\begin{problem}
    Suppose $f$ is an entire function with the property that $\lim_{|z| \to \infty}f(z) = 0$.
    That is,
    for any $\varepsilon > 0$ there exists an $R > 0$ such that for all $z \in \C$ with $|z| > R$ we have $|f(z)| < \varepsilon$.
    Use Liouville's Theorem to show that $f$ must be constant on $\C$,
    and find this value.

    \begin{solution}
        Suppose $f$ is entire with $\lim_{|z| \to \infty}f(z) = 0$.

        $\forall \varepsilon > 0$ there exists $R > 0$ such that $|z| > R$ we have $|f(z)| < \varepsilon$.

        Fix $\varepsilon > 0$ then take $R$ from the definition.
        Then for $z$ in region $\{z\,:\,|z| > R\}$ $|f|$ is bounded by $\varepsilon$.
        Just need to show $f$ is bounded on $\overline{B_{R}(0)} = \{z \in \C\,:\,|z| \leq R\}$.
        By EVT,
        $|F|$ attains a max value,
        say $K$,
        on the compact set $\overline{B_{R}(0)}$.
        In particular,
        for $z \in \C$,
        \[
        |f(z)| \leq \max\{K, \varepsilon\}.
        \]
        So $F$ is bounded on $\C$ and constant by Liouville.
    \end{solution}
\end{problem}

\begin{problem}\phantom{}
    \begin{enumerate}[label = (\alph*)]
        \item Suppose $P(z) = a_dz ^ d + a_{d - 1}z ^ d + \dotsi + a_1z + a_0$ is a non-constant polynomial of degree $d \geq 1$ with real coefficients.

        \begin{enumerate}[label = (\roman*)]
            \item Show that if $w \in \C$ is a root of $P$ then $\bar{w}$ is also a root of $P$.

            \item Hence,
            show that if $P$ has an odd number of roots then it must have a real root.
        \end{enumerate}

        \item Find roots $P(z) = 2z ^ 4 - 4z ^ 3 + 3z ^ 2 + 2z - 2$ given $P(1 + i) = 0$
    \end{enumerate}

    \begin{solution}\phantom{}
        \begin{enumerate}[label = (\alph*)]
            \item
            \begin{enumerate}[label = (\roman*)]
                \item If $P(w) = 0$ then
                \[
                c_dw ^ d + c_{d - 1}w ^ {d - 1} + \dotsi + c_1w + c_0 = 0.
                \]
                \begin{align*}
                    P(\bar{w}) &= c_d\bar{w} ^ d + c_{d - 1}\bar{w} ^ {d - 1} + \dotsi + c_1\bar{w} + c_0 \\
                    &= \overline{\bar{c}_dw ^ d + \bar{c}_{d - 1}w ^ {d - 1} + \dotsi + \bar{c}_1w + \bar{c}_0} \\
                    &= \overline{c_dw ^ d + c_{d - 1}w ^ {d - 1} + \dotsi + c_1w + c_0} \\
                    &= \bar{0} = 0.
                \end{align*}

                \item 
            \end{enumerate}

            \item By part a $P(\overline{1 + i}) = 0$ so $P(1 - i) = 0$ and
            \begin{align*}
                P(z) &= 2(z - (1 + i))(z - (1 - i))(z - a)(z - b) \\
                &= 2(z ^ 2 - (a + b)z + ab)(z ^ 2 - 2z + 2).
            \end{align*}
            Comparing $z ^ 3$ and constant term gets us $ab = -1 / 2$,
            $a + b = 0$ getting $a = 1 / \sqrt{2}, b = -1 / \sqrt{2}$.
        \end{enumerate}
    \end{solution}
\end{problem}

\begin{problem}
    Prove that the function $f : \C \setminus \{0\} \to \C$ defined by $f(z) = \frac{\sin{z}}{z}$ has an analytic continuation to $\C$.

    \begin{solution}
        Start with $\sin{z} = \frac{e ^ {iz} - e ^ {-iz}}{2i}$.
        \begin{align*}
            \sin(z) &= \frac{1}{2i}\left(\sum_{n = 0}^{\infty}\frac{(iz) ^ n}{n!} - \sum_{n = 0}^{\infty}\frac{(-iz) ^ n}{n!}\right) \\
            &= \frac{1}{2i}\sum_{n = 0}^{\infty}\frac{i ^ n - (-i) ^ n}{n!}z ^ n \\
            &= \frac{1}{2i}\sum_{k = 0}^{\infty}\frac{i ^ {2k + 1} - (-i) ^ {2k + 1}}{(2k + 1)!}z ^ {2k + 1} \\
            &= \sum_{k = 0}^{\infty}\frac{(-1) ^ k}{(2k + 1)!}z ^ {2k + 1}.
        \end{align*}
        Thus
        \[
        \frac{\sin{z}}{z} = \sum_{k = 0}^{\infty}\frac{(-1) ^ k}{(2k + 1)!}z ^ {2k}
        \]
        converges on $\C$.
        
    \end{solution}
\end{problem}

\begin{problem}
    Find a holomorphic function $f : B_1(0) = \{z \in \C\,:\,|z| < 1 \to \C\}$ such that
    \[
    f(x) = |x|,\quad x \in (-1, 1),
    \]
    or explain why no such function exists.

    \begin{solution}
        Note that $f(z) = z$ on $(0, 1)$,
        $f(z) = -z$ on $(-1, 0)$.
        So
        \begin{align*}
            S_1 &= \{z \in B_1(0)\,:\, f(z) = z\} \supset (0, 1) \\
            S_2 &= \{z \in B_1(0)\,:\, f(z) = -z\} \supset (-1, 0).
        \end{align*}
        Note $z = 0$ is an accumulation point of $S_1$ and $S_2$.
        By identity theorem,
        $f(z) = z$ on $B_1(0)$ and $f(z) = -z$ on $B_1(0)$.
        Contradiction.
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$8$}{}}

\begin{problem}
    Find the winding number of the following contour $\gamma : [0, 2\pi] \to \C$ around the point $z = 0$
    \[
    \gamma(t) = e ^ {e ^ {it}}.
    \]

    \begin{solution}
        \[
        \gamma(t) = e ^ {e ^ {it}} = e ^ {\cos{t} + i\sin{t}} = e ^ {\cos{t}}e ^ {i\sin{t}} = 0 + r(t)e ^ {i\theta(t)}.
        \]
        In right form with $r(t) = e ^ {\cos{t}} > 0$,
        $\theta(t) = \sin{t}$ continuous
        \[
        I_{\gamma}(0) = \frac{\theta(2\pi) - \theta(0)}{2\pi} = \frac{\sin(2\pi) - \sin(0)}{2\pi} = 0.
        \]
    \end{solution}
\end{problem}

\begin{problem}
    Assume $\gamma : [0, 2\pi] \to \C$ and $\alpha : [0, 2\pi] \to \C$ are two closed contours not passing through the origin,
    and let $\gamma\alpha : [0, 2\pi] \to \C$ be the closed contour defined by $(\gamma\alpha)(t) = \gamma(t)\alpha(t)$.
    Show that
    \[
    I_{\gamma\alpha}(0) = I_{\gamma}(0) + I_{\alpha}(0),
    \]
    using the property that $I_{\gamma}(w) = \frac{1}{2\pi i}\int_{\gamma}\frac{1}{z - w}\,dz$.

    \begin{solution}
        Want to show $I_{(\gamma\alpha)}(0) = I_{\gamma}(0) + I_{\alpha}(0)$.
        By a lemma from the lecture notes
        \begin{align*}
            I_{(\gamma\alpha)}(0) &= \frac{1}{2\pi i}\int_{(\gamma\alpha)}\frac{1}{z}\,dz \\
            &= \frac{1}{2\pi i}\int_{0}^{2\pi}\frac{(\gamma\alpha)'(t)}{(\gamma\alpha)(t)}\,dt \\
            &= \frac{1}{2\pi i}\int_{0}^{2\pi}\frac{\gamma'(t)\alpha(t) + \gamma(t)\alpha'(t)}{\gamma(t)\alpha(t)}\,dt \\
            &= \frac{1}{2\pi i}\int_{0}^{2\pi}\frac{\gamma'(t)}{\gamma(t)}\,dt + \frac{1}{2\pi i}\int_{0}^{2\pi}\frac{\alpha'(t)}{\alpha(t)}\,dt \\
            &= \frac{1}{2\pi i}\int_{\gamma}\frac{1}{z}\,dz + \frac{1}{2\pi i}\int_{\alpha}\frac{1}{z}\,dz \\
            &= I_{\gamma}(0) + I_{\alpha}(0).
        \end{align*}
    \end{solution}
\end{problem}

\begin{problem}
    We define the complex-valued function $\arctan$ by the formula
    \[
    \arctan(z) = \frac{i}{2}\Log(1 - iz) - \frac{i}{2}\Log(1 + iz).
    \]
    Here,
    $\Log$ denotes the principal branch of the logarithm.

    \begin{enumerate}[label = (\alph*)]
        \item Find the largest domain $D \subset \C$ on which $\arctan(z)$ is holomorphic and calculate $\arctan(1)$.

        \item Let $\gamma$ be the curve which traces
        (anti-clockwise)
        the ellipse $\frac{x ^ 2}{2} + 2y ^ 2 = 1$ in $\C$.
        Use part (a) to show that
        \[
        \int_{\gamma}\frac{\arctan(z)}{z - 1}\,dz = \frac{i\pi ^ 2}{2}.
        \]
    \end{enumerate}

    \begin{solution}\phantom{}
        \begin{enumerate}[label = (\alph*)]
            \item This is holomorphic when $\Log(1 - iz)$ and $\Log(1 + iz)$ are both holomorphic.
            $\Log(w)$ is holomorphic on $\C \setminus \R_{\leq 0}$.
            So $\Log(1 - iz)$ is holomorphic whenever $1 - iz \neq t$,
            $t \leq 0$,
            so
            \begin{align*}
                \iff i + z &\neq it& t &\leq 0 \\
                \iff z &\neq (t - 1)i& t &\leq 0 \\
                \iff z &\neq si & s &\leq -1.
            \end{align*}
            Similarly $\Log(1 + iz)$ is holomorphic whenever $1 + iz \neq t$,
            $t \leq 0$.

            \[
            1 - z \neq it \iff z \neq (1 - t)i \iff z \neq si
            \]
            with $s \geq 1$.
            Combining them we get the region that $\arctan$ is holomorphic on.

            \item $f(z) > \arctan(z)$ is holomorphic on and inside the ellipse $\gamma$,
            so
            \[
            \int_{\gamma}\frac{\arctan(z)}{z - 1}\,dz = 2\pi if(1) = 2\pi i\arctan(1) = \frac{i\pi ^ 2}{2}.
            \]
        \end{enumerate}
    \end{solution}
\end{problem}

\begin{problem}
    Compute the principal part of the LS of the following functions on $B_{1}^{*}(0) = B_1(0) \setminus \{0\}$.
    State the type of isolated singularity the function has at $z = 0$,
    \[
    f(z) = \frac{e ^ z}{z ^ 4}.
    \]

    \begin{solution}
        \begin{align*}
            f(z) &= \frac{e ^ z}{z ^ 4} \\
            &= \frac{1}{z ^ 4}\sum_{n = 0}^{\infty}z ^ n \\
            &= \sum_{n = 0}^{\infty}\frac{z ^ {n - 4}}{n!} \\
            &= \sum_{m = -4}^{\infty}\frac{z ^ m}{(m + 4)!} &(m = n - 4)
        \end{align*}
        \[
        \mathrm{PP}(z) = \sum_{m = -4}^{-1}\frac{z ^ m}{(m + 4)!} = z ^ {-4} + z ^ {-3} + \frac{1}{2}z ^ {-2} + \frac{1}{6}z ^ {-1}.
        \]
        Pole of order $4$ at $z = 0$.
    \end{solution}
\end{problem}

\begin{problem}
    Find the annulus of convergence of
    \[
    \sum_{n = -\infty}^{\infty}\frac{z ^ n}{2 ^ {|n|}}.
    \]

    \begin{solution}
        \begin{align*}
            \sum_{n = -\infty}^{\infty}\frac{z ^ n}{2 ^ {|n|}} &= \sum_{n = -\infty}^{-1}\frac{z ^ n}{2 ^ {|n|}} + \sum_{n = 0}^{\infty}\frac{z ^ n}{2 ^ {|n|}} \\
            &= \sum_{n = -\infty}^{-1}(2z) ^ n + \sum_{n = 0}^{\infty}\left(\frac{z}{2}\right) ^ n \\
            &= \sum_{m = 1}^{\infty}\left(\frac{1}{2z}\right) ^ m + \sum_{n = 0}^{\infty}\left(\frac{z}{2}\right) ^ n &(m = -n)
        \end{align*}
        converges when $|1 / (2z)| < 1 \iff |z| > 1 / 2$ and $|z| < 2$ so
        \[
        \frac{1}{2} < |z| < 2.
        \]

        On the annulus,
        \[
        f(z) = \sum_{m = 0}^{\infty}\left(\frac{1}{2z}\right) ^ m - 1 + \sum_{n = 0}^{\infty}\left(\frac{z}{2}\right) ^ n = \frac{1}{1 - \frac{1}{2z}} - 1 + \frac{1}{1 - \frac{z}{2}} = \frac{3z}{(2z - 1)(2 - z)}.
        \]
    \end{solution}
\end{problem}

\newpage

\section{Problem Class \texorpdfstring{$9$}{}}

\begin{problem}
    Suppose $D \subseteq \C$ is a simply connected domain,
    and let $a \in D$.
    Assume $f : D \setminus \{a\} \to \C$ is holomorphic and has a removable singularity at $z = a$.
    Calculate
    \[
    \int_{\gamma}f(z)\,dz,
    \]
    where $\gamma$ is any closed contour in $D \setminus \{a\}$.

    \begin{solution}
        $f$ is holomorphic on $D \setminus \{a\}$ with removable singularity at $z = a$.
        By a lemma from the lecture notes,
        $f$ extends to a holomorphic function on $D$,
        call this function $g$.
        Then
        \[
        \int_{\gamma}f(z)\,dz = \int_{\gamma}g(z)\,dz = 0
        \]
        by Cauchy-Goursat for all closed contours.
    \end{solution}
\end{problem}

\begin{problem}
    Determine all zeros and isolated singularities
    (with orders where appropriate)
    of the following
    \[
    f(z) = \frac{ze ^ {iz}}{\sin(z)}.
    \]

    \begin{solution}
        \[
        f(z) = \frac{ze ^ {iz}}{\sin(z)} \eqqcolon \frac{g(z)}{h(z)}.
        \]
        By result in lectures,
        zeros of $f$ occur when $g = 0$ but $h \neq 0$,
        poles of $f$ do occur when $h = 0$ but $g \neq 0$.
        Zeros of $g$ are just $z = 0$ since $e ^ {iz} \neq 0$.
        Zeros of $h$ are $z = n\pi$,
        for $n \in \Z$.
        Thus $f$ has pole at $z = n\pi$,
        $n \neq 0$,
        and these are order $1$ since $h'(n\pi) = \cos(n\pi) \neq 0$.
        No zeros.
        Investigate $z = 0$.
        We have
        \[
        \lim_{z \to 0}zf(z) = \lim_{z \to 0}\frac{e ^ 2e ^ {iz}}{\sin(z)} = \lim_{z \to 0}\frac{2ze ^ {iz} + iz ^ 2e ^ {iz}}{\cos(z)} = \frac{0 + 0}{1} = 0
        \]
        so removable singularity.
    \end{solution}
\end{problem}

\begin{problem}
    Let $f$ be a non-constant entire function.
    so that $g(z) \coloneqq f(1 / z)$ is holomorphic on $\C \setminus \{0\}$.

    \begin{enumerate}[label = (\roman*)]
        \item Suppose $f$ is a polynomial of degree $n$.
        What type of isolated singularity would $g$ have at $z = 0$?

        \item Assume $f$ is 'transcendental',
        that is,
        the Taylor series expansion $f(z) = \sum_{n = 0}^{\infty}a_nz ^ n$ contains infinitely many $a_n \neq 0$.
        What type of isolated singularity would $g$ have at $z = 0$?

        \item Using the Great Picard Theorem,
        show that $f$ is not a polynomial,
        then the image of $f$ contains $\C \setminus \{b\}$ for some $b \in \C$.

        \item If $f$ is a polynomial then what is the image of $f$?
    \end{enumerate}

    \begin{solution}
        $f$ non-constant and entire,
        define $g(z) = f\left(\frac{1}{z}\right)$ which is holomorphic on $\C \setminus \{0\}$.
        
        \begin{enumerate}[label = (\roman*)]
            \item Suppose $f$ is a polynomial,
            so $f(z) = c_0 + c_1z + \dotsc + c_nz ^ n$.
            Then
            \begin{align*}
                g(z) &= f\left(\frac{1}{z}\right) \\
                &= c_0 + \frac{c_1}{z} + \dotsi + \frac{c_n}{z ^ n} \\
                &= c_0 + \sum_{k = 1}^{n}c_kz ^ {-k} \\
                &= c_0 + \sum_{m = -n}^{-1}c_{-m}z ^ m
            \end{align*}
            so $g$ has a pole of order $n$ at $z = 0$.

            \item Suppose $f$ is "transcendental",
            that is the Taylor series contains infinitely many terms,
            so $f(z) = \sum_{n = 0}^{\infty}c_nz ^ n$.
            In this case
            \begin{align*}
                g(z) &= f\left(\frac{1}{z}\right) \\
                &= \sum_{n = 0}^{\infty}c_n\left(\frac{1}{z}\right) ^ n \\
                &= \sum_{m = -\infty}^{0}c_{-m}z ^ m
            \end{align*}
            principle part contains infinitely many terms so essential singularity.

            \item Find image of $f$ when $f$ is transcendental.
            We know $g(z) = f(1 / z)$ has an essential singularity at $z = 0$.
            We have
            \[
            f(z) = g\left(\frac{1}{z}\right) = (g\circ h)(z),
            \]
            where $h(z) = \frac{1}{z}$.
            $|z| = 1$ then $|h(z)| = \frac{1}{|z|} = 1$.
            By Great Picard Theorem,
            there exists $b \in \C$ such that $g(B_1(0)) \supset \C \setminus \{b\}$.
            Thus $f : \C \setminus \overline{B_1(0)} \to \C \setminus \{b\}$ surjectively.

            \item We've just seen if $f$ is not a polynomial then $f(\C) \supset \C \setminus \{b\}$.
            What if $f$ is a polynomial
            (non-constant)?
            Let $w \in \C$.
            When can we find $z_0 \in \C$ such that $f(z_0) = w$?
            Let $g(z) = f(z) - w$,
            then $g$ is a non-constant polynomial and we seek a zero of $g$.
            By FTA,
            every polynomial has a root in $\C$,
            so $\exists z_0 \in \C$ such that $g(z_0) = 0$ so $f(z_0) = w$.
            So $f(\C) = \C$.
        \end{enumerate}
    \end{solution}
\end{problem}

\begin{problem}
    Evaluate the contour integral of the following function around the contour $\gamma$ with equation $|z| = 2$ of
    \[
    \int_{\gamma}\frac{1}{(z ^ 2 - 1) ^ 2}\,dz.
    \]

    \begin{solution}
        Note
        \[
        f(z) = \frac{1}{(z ^ 2 - 1) ^ 2} = \frac{1}{(z - 1) ^ 2(z + 1) ^ 2}.
        \]
        Thus,
        $f$ has poles of order $2$ at $z = \pm 1$
        (because they are zeros of order $2$ of the denominator).
        By CRT
        \[
        \int_{|z| = 2}f(z)\,dz = 2\pi i(\Res_{z = 1}(f) + \Res_{z = -1}(f)).
        \]
        We can write $f(z) = \frac{g_1(z)}{(z - 1) ^ 2}$ where $g_1(z) = \frac{1}{(z + 1) ^ 2}$ holomorphic at $z = 1$.
        By Rule $3$,
        \[
        \Res_{z = 1}(f) = \frac{g_1'(1)}{1!} = g_1'(1) =  -\frac{2}{2 ^ 3} = -\frac{1}{4}
        \]
        since $g)1''(z) = -\frac{2}{(z + 1) ^ 3}$.
        Similarly,
        $\Res_{z = -1}(f) = \frac{1}{4}$,
        so integral is $0$.
    \end{solution}
\end{problem}

\begin{problem}
    By converting it into a complex contour integral of some meromorphic function,
    evaluate the real integral
    \[
    \int_{0}^{\pi}\frac{1}{4 + \sin ^ 2(\theta)}\,d\theta.
    \]

    \begin{solution}
        \[
        \sin(\theta) = \frac{e ^ {i\theta} - e ^ {-i\theta}}{2i}
        \]
        \begin{align*}
            \int_{0}^{\pi}\frac{1}{4 + \sin ^ 2(\theta)}\,d\theta &= \int_{0}^{\pi}\frac{1}{4 - \frac{1}{4}\left(e ^ {2i\theta} + e ^ {-2i\theta} - 2\right)}\,d\theta \\
            &= \int_{0}^{\pi}\frac{4e ^ {2i\theta}}{16e ^ {2i\theta} - e ^ {4i\theta} - 1 + 2e ^ {2i\theta}}\,d\theta \\
            &= \int_{0}^{\pi}\frac{-2i\gamma'(\theta)}{18\gamma(\theta) - \gamma(\theta) ^ 2 - 1}\,d\theta &(\gamma(\theta) = e ^ {2i\theta},\quad \theta \in [0, \pi], \gamma'(\theta) = 2ie ^ {2i\theta}) \\
            &= \int_{|z| = 1}\frac{-2iz}{18z - z ^ 2 - 1}\,dz \\
            &= \int_{|z| = 1}\frac{2iz}{z ^ 2 - 18z + 1}\,dz \\
            &= \int_{|z| = 1}\frac{2iz}{(z - (9 - \sqrt{80}))(z - (9 - \sqrt{80})}\,dz \\
            &= 2\pi i\Res_{z = 9 - \sqrt{80}}(f) = \frac{\pi}{2\sqrt{5}}.
        \end{align*}
    \end{solution}
\end{problem}

















\end{document}